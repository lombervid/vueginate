{
  "name": "@vueginate/root",
  "packageManager": "pnpm@8.1.1",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
